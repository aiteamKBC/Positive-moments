"""
Phase 4A: the orchestration layer.

Everything here COORDINATES. Nothing here decides what a checklist item is,
what a roster looks like, whether a lecture is Perfect, or how a legacy row is
shaped. Those answers already exist in validated services, and a second
implementation of any of them is the one failure mode this package must never
have.
"""
