# Controlled migration phases

0. **Architecture baseline** — inventory current systems, define safety boundaries, configuration, logging, errors, and timezone policy.
1. **Lecture Discovery + Registry** — app-only Graph calendar discovery, exact online-meeting resolution in the discovery mailbox's user context, exact active Aptem matching, canonical occurrence identity, audit runs, manual CLI, and shadow validation. Complete: see [phase1_final_hardening.md](phase1_final_hardening.md).
2. **Transcript Platform**
   - **2A - Raw acquisition (complete).** Every Microsoft Graph transcript artifact for a downstream-ready canonical lecture is acquired verbatim into `lecture_transcript_artifacts`, with SHA-256-verified content versions in `lecture_transcript_artifact_contents` and audit rows in `lecture_transcript_acquisition_runs`. No selection, combination, timeline shifting, or cue parsing. See [phase2a_raw_transcripts.md](phase2a_raw_transcripts.md).
   - **2B - Selection and combination (complete).** The legacy QA One Lecture selector and VTT combiner are ported from the authoritative export and reproduce 6/6 legacy session IDs for 2026-09-04 from persisted evidence alone, with zero Graph calls. Real multi-part validation is still outstanding. See [phase2_transcript_selection_reference.md](phase2_transcript_selection_reference.md).
   - **2C1 - Canonical cue model (complete).** The Phase 2B combined WebVTT is parsed into `lecture_transcript_documents` and `lecture_transcript_cues` with provenance-keyed identity and exact duration parity. Raw speaker labels only; no identity inference. See [phase2c1_canonical_cues.md](phase2c1_canonical_cues.md).
   - **2C2 - Speaker inventory (complete).** Distinct raw WebVTT labels become one deterministic row per label per document in `lecture_transcript_speakers`, with cue aggregates and a conservative normalized form. No person, role, attendance or engagement inference. See [phase2c_speaker_attendance_reference.md](phase2c_speaker_attendance_reference.md).
   - **2C3 - Attendance resolution and deterministic roles (complete).** The `kbc_attendance` roster (date + normalized module + `Attendance = 1`) is frozen into fingerprinted snapshots; speakers are resolved exact -> normalized-exact -> legacy `isSimilar`, with an ambiguity guard; the VTT top speaker is stored as `TRAINER_CANDIDATE` in a separately versioned roles table. No AI trainer override, no engagement. See [phase2c3_attendance_and_roles.md](phase2c3_attendance_and_roles.md).
   - **2C4 - Deterministic engagement (complete).** `legacy_qa_v8_engagement_v1` computes `spoke / attended`, score bands, the Item 7 recommendation and silent learners from persisted 2C3 evidence only (no live attendance read, no QA write). Roster rule `attendance_roster_v2_exclude_makeup` excludes post-lecture makeup rows, giving 6/6 legacy parity; v1 evidence is retained. See [phase2c4_engagement.md](phase2c4_engagement.md).
   - **Phase 3A - Shadow QA engine (built; AI half unproven).** Legacy prompt/schema ported verbatim (gpt-5.2 agent, gpt-4.1 structured-output fixer), deterministic delivery gate and Items 1/2/7, evidence-timestamp validation, shadow-only persistence. Live gpt-5.2 validated (Phase 3A.1): 6 real calls, 255 validated evidence clips, 47/48 AI-only agreement, 0-call reuse. Item 2 intentionally diverges from legacy, which had a 3-hour timezone defect. See [phase3a_shadow_qa.md](phase3a_shadow_qa.md).
   - **Phase 3B - Output rendering (complete).** Legacy evidence renderer (`blocksFromRange`, 8 blocks, 260 chars, 1500 ms merge) over canonical cues, versioned read-only LMS snapshot, and the legacy session/checklist compatibility shape - all shadow, zero model calls. See [phase3b_output_rendering.md](phase3b_output_rendering.md).
   - **Phase 3C1 - Writer guardrails (complete, dry-run only).** Mode-gated legacy writer with ownership tracking in `lecture_qa_legacy_writes`, atomic per-lecture writes with re-read verification, ownership-scoped rollback, and a bounded 3-generation model cap. No legacy row written. See [phase3c1_legacy_writer.md](phase3c1_legacy_writer.md).
   - **Phase 3C2 - Controlled real canary (complete).** One new delivered lecture (Ray | PMP - June 2026, 2026-09-16) was prepared through 2A->3B and written to the legacy QA tables under `CANARY_NEW_ONLY` with an explicit lecture id and write confirmation: +1 session, +11 checklist rows, +1 ownership row, 22/22 and 66/66 field parity, zero foreign-column change, and `WOULD_SKIP_IDENTICAL` on a second identical run. A real generation-1 `INVALID_STRUCTURED_OUTPUT` was recovered by the bounded retry without loosening the validator. See [phase3c2_real_canary.md](phase3c2_real_canary.md).
   - **Phase 3C2.1 - n8n coexistence audit (complete, read-only).** The QA Master was located at `automation/legacy_n8n/QA Master Daily — Safe Exact Recording v8` (no file extension, which is why the Phase 3C2 `*.json` scan missed it). Its QA branch is today-only in Africa/Cairo on cron `0 21,23 * * *` with no date parameter, so the canary is safe from scheduled and manual Master runs; the one real overwrite path is direct invocation of the child, whose 22+6 upsert columns are byte-identical to the coded writer's and which has zero ownership awareness. Recommended protection: an ownership check immediately before the child's upserts. See [phase3c2_1_n8n_coexistence.md](phase3c2_1_n8n_coexistence.md).
   - **Phase 3C2.2 - n8n ownership guard (repository complete, live deployment pending).** Four nodes added to QA One Lecture v8 so both QA upserts sit behind one parameterised `lecture_qa_legacy_writes` / `write_status = 'WRITTEN'` check, fail-closed on any error or non-boolean result, preserving all 11 checklist items. No legacy node changed. Live deployment attempted 2026-09-17 with a working management API and stopped without changing anything: `PUT /api/v1/workflows/{id}` is the only update operation and its `workflowSettings` schema (`additionalProperties: false`) cannot carry the live workflow's `binaryMode` and `availableInMCP`, which are per-workflow configuration rather than defaults. Deploy via the n8n UI, or authorise dropping those two settings. Live backup saved. See [phase3c2_2_ownership_guard.md](phase3c2_2_ownership_guard.md).
   - **Phase 3C2.3A - Multi-part seam and timeline (complete, investigation only).** Andrew's 167-minute cue origin is correct provider call-relative semantics, present in part 1's RAW VTT with offset 0, and matched by 103 existing legacy production sessions; the legacy combiner provably preserves it. The 61 s seam is real duplicate audio coverage (164.7% speech density vs a 89.9% single-part median), which legacy does not deduplicate either. Classification MULTIPLE_ISSUES; a versioned seam-dedup and an additive presentation coordinate are designed but not implemented. `planner._zone` is broken for any non-zero origin. See [phase3c2_3a_multipart_timeline.md](phase3c2_3a_multipart_timeline.md).
   - **Phase 3C2.3B - Versioned seam dedup (complete).** New parser version `webvtt_canonical_v2_seam_dedup` with policy `earlier_part_wins_v1`, built from the selected parts and their raw artifacts rather than the flattened transcript. No migration needed - the existing provenance key already includes `parser_version`. Andrew rebuilt through 2C4: 601 -> 592 cues, 9 cross-part duplicates removed (1 straddling, 11,755 ms tail), seam speech density 164.7% -> 82.1%, 0 backward timeline steps, origin and duration unchanged, engagement unchanged. v1 immutable; 13/13 single-part lectures reproduce v1 exactly. See [phase3c2_3b_seam_dedup.md](phase3c2_3b_seam_dedup.md).
   - **Phase 3C2.3C - Multi-part QA, rendering and dry run (complete).** Andrew evaluated on the v2 seam-deduplicated document: COMPLETED on generation 1, 11/11 Met, 43 valid clips spanning both parts, 0 clips citing a removed seam cue, 0 rendered timestamps inside the removed window, RENDERED with 11 rows and 134 blocks, writer DRY_RUN = WOULD_INSERT with zero blocking conditions and zero writes. Fixed a real defect the guard caught first: the trainer lateral in `LOAD_QA_INPUTS` matched on snapshot but not document, so two parser versions fanned the input out. Open decision: Andrew is all-Met, which legacy would accompany with a `qa_perfect_lectures` row the coded writer does not create. See [phase3c2_3c_multipart_qa.md](phase3c2_3c_multipart_qa.md).
   - **Phase 3C2.3D - Perfect Lecture parity and safe writer design (complete).** The open decision above is now answered. Legacy's rule transcribed from the executable child (`metCount === 11 && uniqueOrders.size === 11 && allMet && cancelled !== true`, key `date|subject`) and implemented as versioned eligibility `legacy_qa_v8_perfect_v1`, derived only from the frozen Phase 3B result. Two additive coded tables (migration 015) hold the answer and the legacy-row ownership; nothing was added to `qa_perfect_lectures`. Transaction design is Option B - QA commits first, the Perfect Lecture is a separate idempotent derived output. Andrew is `is_perfect = true` / `PERFECT_WOULD_INSERT`; the first canary is `PERFECT_NOT_ELIGIBLE`, matching legacy. Found a third active consumer nobody had audited: `QA Perfect Lectures - Excel Sync` runs every 15 minutes and publishes a perfect row to a shared Excel workbook once its `recording_url` is filled, so a Perfect Lecture write has an outward-facing consequence that deleting the row does not undo. No row written. See [phase3c2_3d_perfect_lecture.md](phase3c2_3d_perfect_lecture.md).
   - **Phase 3C2.3E - Real multi-part + Perfect Lecture production canary (written, one defect open).** Andrew written for real under `CANARY_NEW_ONLY`: `qa_doctors_sessions` +1, `qa_doctors_checklist_items` +11, `lecture_qa_legacy_writes` +1, `qa_perfect_lectures` +1, `lecture_perfect_lecture_results` +1, `lecture_perfect_lecture_legacy_writes` +1. QA verified 22/22 and 66/66, post-write digest `83d2be26...`; Perfect verified 10/10 coded-owned fields, digest `78fad6a7...`. A second identical run is `WOULD_SKIP_IDENTICAL` on both targets with zero writes. `recording_url` and `excel_synced_at` both NULL, so Excel Sync's WHERE selects nothing. **Open defect:** the Perfect row carries `attended_count = NULL` where legacy writes 7 - `LOAD_RENDERED` never selected the column. The loader is fixed and a regression test added, but the written row is unrepaired because idempotency keys on the unchanged source fingerprint, not the mapping. See [phase3c2_3d_perfect_lecture.md](phase3c2_3d_perfect_lecture.md) section 11.
   - **Phase 3C2.3F - Perfect Lecture mapping repair and mapping-aware idempotency (complete).** Andrew's `attended_count` repaired NULL -> 7 through `EXPLICIT_BACKFILL --allow-update-existing`, derived from the persisted rendered payload, with no manual SQL and no rollback: one row updated, QA untouched (digest `83d2be26...`, 22/22, 66/66), foreign columns byte-preserved, and the same single ownership and result identity. Gate F closed: a coded-owned target is identical only when the source fingerprint, the target's mapped digest AND the recorded mapped digest all agree, over coded-owned columns plus a new `legacy_qa_v8_perfect_mapping_v2` contract version held in existing metadata jsonb (no migration). A second run is a NOOP on both targets. See [phase3c2_3d_perfect_lecture.md](phase3c2_3d_perfect_lecture.md) section 12.
   - **Phase 3C3A - Controlled full-day QA pilot, 2026-09-16 (complete).** All 7 canonical lectures of one real day now carry coded QA. The 5 remaining candidates were processed and written one at a time: 6 provider calls (one KSB-enum retry on Steve-EVM, inside the cap, no validation loosened), 5/5 Phase 3A COMPLETED, 5/5 rendered, 3 Perfect and 2 not, 5/5 written with 22/22 and 66/66 verification and proven idempotent before the next write. Day deltas +5 / +55 / +3 / +5 / +3 / +3, both canaries byte-unchanged, no date-wide command, no unowned legacy row touched. Item 2 observability: every lecture actually started within 2 minutes of schedule, but on **2 of 7** the call opened 22 and 167 minutes early - Item 2 measures the call, so it would mask a genuinely late start. Recovery model: all 14 stages resolve for all 7 lectures; the one gap is that a NOT_ELIGIBLE Perfect answer is not persisted. See [phase3c3a_controlled_day_pilot.md](phase3c3a_controlled_day_pilot.md).
   - **Phase 3C3B - Fresh-day readiness hardening (complete).** The pilot's three prerequisites closed. **A:** `calculate-engagement --lecture-id` is a separate SQL statement with no `session_date` predicate, so lecture mode cannot widen; proven on Steve-EVM with 1 row touched, 0 siblings, values unchanged. **B:** Perfect eligibility is now persisted for every finalized render (4 -> 14 results, 5 perfect / 9 not; no legacy table changed), and a second defect found while proving it - `computed_at` advancing on every rerun - was fixed so a rerun is byte-identical. **C:** Item 2's punctuality source is now versioned: `legacy_call_bounds_v1` preserved, `canonical_cue_bounds_v1` adopted for new evaluations. Read-only replay over all 14 selected lectures: computable 14/14, start divergence median 2 min and >20 min on 2, end divergence >20 min on 3, and **1 status change** - a NON_DELIVERED lecture flipping Met -> Not Met because a call left open had been hiding a 107-minute-early finish. No production row rewritten. See [phase3c3b_fresh_day_readiness.md](phase3c3b_fresh_day_readiness.md).
   - **Phase 3C3C - Second controlled fresh day, 2026-09-17, end to end (complete, 1 lecture unresolved).** First run of the whole coded pipeline from Microsoft Graph discovery through legacy compatibility writing. Discovery found exactly the 3 expected lectures out of 13 calendar events (9 excluded as `NOT_AN_ACTIVE_APTEM_GROUP`); 25 artifacts acquired; selection narrowed 15->1 and 9->1, all single-part; 3 canonical documents with 0 backward steps. Lecture-scoped engagement proven fresh: 1 row touched per run, 0 siblings, and no later run disturbed an earlier one. All new evaluations bound to `canonical_cue_bounds_v1`; no Item 2 status differed from the legacy source on this day. 2 of 3 lectures written (+2 / +22 / +1 / +2 / +2 / +1), both verified 22/22 and 66/66 and idempotent; both canaries byte-unchanged. **Root cause found for the recurring KSB failures:** the provider sends `response_format: json_object`, so the schema's enum is never provider-enforced - it has now cost 4 of 8 first generations and blocked G2 Keith at `MAX_GENERATIONS_EXHAUSTED`. Also found: 2 of 3 lectures have no attendance source data at all, and Martech reached Perfect with an AI-sourced Item 7 because the deterministic override could not apply. Item 2 backfill recommendation: KEEP_VERSIONED_HISTORY. See [phase3c3c_fresh_day_end_to_end.md](phase3c3c_fresh_day_end_to_end.md).
   - **Phase 3C3D - Unattended QA readiness and recovery validation (complete).** Three carried gates closed, then proven on the lecture the pilots could not finish. **H:** the provider now receives `response_format: json_schema` with `strict: true`, a schema derived mechanically from `STRUCTURED_OUTPUT_SCHEMA` (every property required, `additionalProperties: false`, `speaker`/`reasoning` nullable, the twelve unsupported bounds dropped and named in provenance); the local validator is unchanged and still runs, and `K` is never translated. The contract version `strict_json_schema_v1` joins the QA source fingerprint, so the 3 spent `json_object_v1` generations stay spent and the new contract opens its own budget, still capped at 3. **I/J:** `attendance_coverage_v1` derives SOURCE_AVAILABLE_WITH_MEMBERS / SOURCE_AVAILABLE_CONFIRMED_ZERO / SOURCE_MISSING / SOURCE_PARTIAL_OR_INVALID / SOURCE_UNKNOWN from counts the snapshots already froze - no schema, no external read - and a confirmed zero is claimed only when the source itself says so; engagement exposes `ATTENDANCE_SOURCE_MISSING`, and the new opt-in policy `kbc_perfect_v2_attendance_required` holds an otherwise-perfect lecture at the explicitly non-final `PENDING_ATTENDANCE_DATA`, decided BEFORE the supersede branch so a pending state can never mark an owned row. **K:** `provider_attempts` was the HTTP retry counter of the last call, not the generation count; it now stores the generation number, keeps transport retries beside it, and `sync_provider_attempts` repaired G2 Keith 1 -> 3 without touching an attempt row. New `qa-recovery-state --lecture-id` answers the whole resume question per generation contract. **G2 Keith recovered:** resumed at Phase 3A with zero Graph calls and all upstream fingerprints reused, COMPLETED on generation 1 of 3 under the strict contract (11/0/0, 49 clips, 0 errors, KSB types `Knowledge`/`Skill`), RENDERED, dry run clean, written lecture-scoped with 22/22 and 66/66, second run all-zero deltas. Deltas +1 / +11 / **0** / +1 / +1 / **0** - the zeroes are the point: an 11/11 lecture produced no `qa_perfect_lectures` row and no Perfect ownership because it has no attendance evidence. Ray, Andrew, Stephen and Martech byte-unchanged; Martech additionally carries a v2 result recording PENDING_ATTENDANCE_DATA as an Operations marker that changes no history. See [phase3c3d_unattended_qa_readiness.md](phase3c3d_unattended_qa_readiness.md).
   - **Phase 3C3E - Attendance policy finalization and lecture-scoped attendance recovery (complete).** `public.kbc_attendance` is settled as an external read-only source whose silence is represented, never repaired or inferred around. **Defaults:** `kbc_perfect_v2_attendance_required` is now what NEW work gets in the planner and both CLI commands; omitting the coverage reader supplies one rather than downgrading the policy, so the only way to decide without attendance evidence is to name v1 explicitly - which stays selectable forever. **Blind spot found:** the roster query filters `Attendance = 1`, so an empty roster could not distinguish silence from a source that had recorded only absences, and SOURCE_AVAILABLE_CONFIRMED_ZERO was unreachable in production. G2 Keith exposed it - `kbc_attendance` holds one row for its module, marked absent. `source_rows_any_status` is now captured (outside the fingerprint, so no snapshot id moved) and that case reads SOURCE_PARTIAL_OR_INVALID / ALL_SOURCE_ROWS_MARKED_ABSENT - deliberately NOT a confirmed zero, because concluding one would need an enrolment cross-reference that is inference rather than evidence. **Backfill:** 49 rows stamped (24 snapshots, 25 engagement) from counts already frozen, 45 WITH_MEMBERS / 4 MISSING, 2 ATTENDANCE_SOURCE_MISSING, **0** claiming a confirmed zero; plus 16 evaluations stamped with `model_input_fingerprint` after field-by-field verification (the one initial refusal was Andrew's seam-dedup document - the guard was right). No production table touched, no timestamp advanced, second run stamped 0. **`recover-attendance --lecture-id`:** a lecture-scoped primitive, not a scheduler, resuming only the stages attendance can invalidate. It is free because attendance never reaches the provider - proven by `qa_model_input_fingerprint`, which licenses reuse of the frozen answer and fails closed with NO_REUSABLE_MODEL_OUTPUT rather than silently buying a generation; its QA service is built with `provider=None`. A deterministic refresh recomputes Item 7 and the counts only, reuses the frozen output, and records full provenance. **G2 Keith, real run:** WAIT_FOR_ATTENDANCE_SOURCE, 0 writes across 12 tables, 0 OpenAI, 0 Graph, no timestamp moved, idempotent. **Arrival path proven** against G2 Keith's real upstream evidence with only the source gateway substituted: PENDING -> ELIGIBLE (Item 7 unchanged, model output byte-identical, 0 provider calls) and PENDING -> NOT_ELIGIBLE (Item 7 Met -> Not Met deterministically, new render 10/1, superseded evaluation byte-identical), both NOOP on a second run. Operations gains a next-action state model; Martech legitimately shows v1 ELIGIBLE+synced beside v2 PENDING. Also fixed: `LOAD_QA_INPUTS` now pins the current attendance snapshot, so a lecture with recovery history is versioned rather than ambiguous - the guard itself is unchanged. See [phase3c3e_attendance_recovery.md](phase3c3e_attendance_recovery.md).
   - **Phase 4A - Coded scheduler, reconciliation and stage-aware orchestration (complete, scheduler NOT enabled).** The validated pipeline became an orchestration system without a single rule being reimplemented: `app/orchestration/` coordinates and nothing else. **State model:** one canonical resolver answers 15 stages per lecture, and a stage is never COMPLETE merely because a row exists - it is judged on version and lineage, so an engagement row from a superseded snapshot, a render of a superseded evaluation and a selection made before its transcript content arrived are all STALE rather than done. Two real lineage traps were found and closed: Andrew holds two canonical documents and therefore two engagement rows on ONE snapshot, so the EVALUATION declares which document is current rather than "newest wins"; and 2026-09-04's legacy rows are n8n's, so `LEGACY_QA_SYNC` reports NOT_APPLICABLE / LEGACY_ROW_NOT_CODED_OWNED instead of putting a "sync this" instruction in front of an operator for a protected row. `WAITING` is what makes unattended operation affordable - a lecture waiting on an external source is not retryable work. **Cost:** exactly one action costs Graph and exactly one costs a generation, both behind hard gates (`allow_provider=False` builds the QA service with `provider=None`); `REFRESH_DETERMINISTIC_QA` stays a different action from `RUN_QA`; an exhausted attempt budget resolves to MANUAL_REVIEW_REQUIRED rather than being handed back as ordinary work. **Never automated:** `SYNC_LEGACY_QA` and `SYNC_PERFECT` - the orchestrator names the work and records OPERATOR_ACTION_REQUIRED. **Safety:** a read-only `GET /api/v1/workflows/8yeJigbj8BCNBMkU` before every production run verifies `Execute QA One Lecture` is still disabled and fails closed on anything else, including a missing node; there is no write verb anywhere in the module and a test asserts it structurally. **Concurrency:** PostgreSQL session-level advisory locks per lecture, non-blocking, released by the server when a connection dies - a crashed cycle cannot leave a lecture stuck. **Semantics:** RETRY only; FORCE_REPROCESS is defined, exposed to Operations, and unreachable from the scheduler. **Migration 016** adds two audit tables holding decisions and scrubbed reason codes - never transcript text, model output or credentials - and no per-stage copy of derived state. **Two defects the live pilot caught:** counting a successful-but-unchanging call as progress made a waiting lecture re-probe on all eight passes (progress now means the state moved); and `graph_calls` read 0 while seven lectures' transcripts were acquired, because no Phase 2A service publishes that field. **Controlled pilot, three cycles through the scheduled entrypoint:** discovery found **7 previously unregistered lectures on 2026-09-18**; cycle 1 (provider gated off) took six of them from nothing to RUN_QA - day-scoped stages ran once and were shared, one lecture went to MANUAL_REVIEW_REQUIRED for an unresolved meeting, one waits on attendance; cycle 2 bought **5 generations**, 3 COMPLETED -> RENDERED -> stopped at the operator handoff and 2 returned INVALID_EVIDENCE having used **1 of 3** attempts each; cycle 3 was **idempotent** - 1 pass, 0 provider calls, 0 row changes across 17 tables. 2026-09-16 and 2026-09-17 were never reprocessed, and no legacy production table moved in any cycle (660 / 7,633 / 145 unchanged, 0 rows touched). `SCHEDULER_ENABLED` remains **false**; the configuration defaults to the legacy Master's own `0 21,23 * * *` Africa/Cairo with a 3-day lookback. See [phase4a_scheduler_orchestration.md](phase4a_scheduler_orchestration.md).
   - **Phase 4B - Automated production sync, evidence hardening and scheduler activation (complete; SCHEDULER_ENABLED=true, OS trigger not installed).** The last gap Phase 4A left: every legacy write was operator-only, so a finished, rendered, verified lecture sat at OPERATOR_ACTION_REQUIRED for ever. **Evidence failure, diagnosed:** two real 2026-09-18 lectures were discarded on generation 1 for two zero-length clips out of 56 and one 1,560 ms clip out of 48, each having produced a complete correct checklist and zero structured-output errors. Classification **MODEL_OUTPUT_INVALID** - the legacy system message states both rules verbatim ("at least 2 seconds", "never return identical start and end timestamps"), so the validator enforced exactly the prompt's own contract and was right. No pre-generation fix exists: JSON Schema cannot express a cross-field `end > start` or a duration minimum, so regenerating only re-rolls the dice at ~50 clips a throw. **Hardening:** `evidence_policy_v2_exclude_degenerate_clips` separates FABRICATED coordinates (out of range, no cue overlap, unparseable - still fatal, because an ungrounded citation makes the whole answer suspect) from DEGENERATE windows (zero-length, too short - *empty* evidence rather than *false* evidence, excluded from the render and from the verdict's fatal set). `app/qa/validation.py` is byte-unchanged, every clip keeps its real status, no timestamp is rewritten, and only VALID clips ever render. The policy is deliberately **NOT** in the source fingerprint: putting it there would have orphaned all 23 existing evaluations and bought ~20 fresh generations on the next cycle. **Free recovery:** `revalidate_evidence` re-judges the STORED raw output under the current policy with `provider=None` - both lectures reached COMPLETED (11/0/0 and 10/1/0) for **zero provider calls**, the attempt history still shows exactly one generation each, and `REVALIDATE_EVIDENCE` is now an action the scheduler tries before anything that costs money. **Automated sync:** `sync_safety.py` names the whole safe set - `WOULD_INSERT`/`WOULD_SKIP_IDENTICAL` and `PERFECT_WOULD_INSERT`/`SKIP_IDENTICAL`/`NOT_ELIGIBLE`/`PENDING_ATTENDANCE_DATA`; everything else, including an unrecognised decision, becomes MANUAL_REVIEW_REQUIRED carrying the writer's own reason code. Every write is planned twice: once under DRY_RUN to get the decision, then again inside the write-enabled writer, which can still refuse. `PERFECT_WOULD_UPDATE` is actionable to the writer, so the Perfect planner is left OUT of the write pass when unsafe rather than trusted to decline. Stage order corrected - PERFECT_ELIGIBILITY now precedes both writes, because it derives from the render and never needed the legacy row, retiring Phase 4A's step-over workaround. **Concurrency:** a second, coarser `SchedulerCycleLock` stops two cycles duplicating day-wide discovery and provider work; per-lecture locks are unchanged. **Controlled cycles:** cycle 1 carried five lectures unattended from their current state to a verified legacy QA row (+5 sessions, +55 checklist rows, +1 Perfect row - the first ever under the attendance-aware policy) with **0 provider calls**; cycles 2 and 3 were idempotent with **zero row changes** across 19 tables. A real Graph timeout during cycle 3's first attempt aborted safely with no partial state, and discovery failures are now non-fatal so one slow calendar query cannot cost a night's reconciliation. **2026-09-18 finished:** 5 complete, 1 waiting on attendance, 1 manual review, **0 failed**; the review lecture is a duplicate calendar booking, now named as such with a pointer to the sibling event that did resolve. **Activation:** SCHEDULER_ENABLED=true, Africa/Cairo, `0 21,23 * * *`, lookback 3 - unchanged. The variable schedules nothing on its own; `automation/scheduler/` provides a Windows Task Scheduler installer (the current local reality, machine already on Egypt Standard Time) and a Compose service, both calling the same `run_cycle`. **The OS trigger is not installed** - that one command remains for the operator. See [phase4b_automated_production_sync.md](phase4b_automated_production_sync.md).
   - **Phase 4C1 - Deterministic duplicate calendar event resolution (complete).** The one thing left on 2026-09-18: two Ray-MSP calendar events for one class, identical normalized subject, module, start and end, different iCalUIDs, one holding a confirmed Teams meeting and one holding nothing. Phase 4B named it correctly and stopped, leaving a MANUAL_REVIEW_REQUIRED nobody could ever clear - re-running discovery gives the same answer and inventing a meeting id is forbidden. **The rule:** a candidate group is exact equality on all five of `(session_date, normalized_subject, module, scheduled_start, scheduled_end)` - the platform's own `normalize_group`, the matched ACTIVE Aptem group, and start AND end to the second. No fuzzy titles, no nearest-start-time, no tolerance window, no model. A **winner** holds a meeting id, a RESOLVED (or pre-migration-002 EXACT_JOIN_URL_MATCH) mapping AND `ORGANIZER_ID_CONFIRMED` - stricter than `downstream_ready` on purpose, because a lecture is about to be declared a duplicate of this one. A **suppressible** sibling holds no meeting and a FINAL unresolved status; `AMBIGUOUS_ONLINE_MEETING` is deliberately excluded, because "Graph found several candidates" is evidence a meeting EXISTS, the opposite of the emptiness the rule needs. **Exactly one shape is automated:** one winner, every other member empty. Two winners with different meetings, two events sharing one meeting, no winner, or a non-suppressible sibling all become MANUAL_REVIEW_REQUIRED with the case named - and in those cases EVERY member reports review, including one that resolved, because two events that both ran the pipeline would each write a legacy row for one real class. A refused group is refused whole; half-deciding it is worse than not deciding it. **Suppression is an annotation, never a deletion.** One merged `metadata` key carries both calendar identities, both iCalUIDs, the winner's meeting id, the group key, the rule version and `resolved_at`; the row keeps its lecture_id, its event id and every discovery diagnostic, and the discovery upsert now carries the key across by name so a nightly rediscovery cannot erase the decision. Three guards on the write: `meeting_id IS NULL`, `NOT (metadata ? 'duplicate_suppression')` (which IS the idempotency - `resolved_at` never churns), and a re-read of the candidate's downstream footprint that refuses outright if it ever produced a transcript, document, evaluation or legacy row. **Structurally unable to enter the pipeline:** the state resolver answers a suppressed row and short-circuits BEFORE any downstream query - every stage NOT_APPLICABLE, NOTHING_TO_DO, complete, no review - so "cannot be processed" is a property of the code path rather than a rule fifteen stages have to remember. `SUPPRESS_DUPLICATE_EVENT` is automatable, lecture-scoped, and in neither GRAPH_ACTIONS nor PROVIDER_ACTIONS: the repair costs one indexed read and one annotation. **Reconciliation** gives it its own bucket - visible for audit, excluded from failed, review, complete and every derived counter, with `suppressed_duplicate_count` and `business_lecture_count` alongside the canonical count. **The registry-wide scan is read-only and refuses `--execute`;** it found two duplicate groups in the entire history, both the same recurring Ray booking, both Case A, zero needing a human. Only 2026-09-18 was suppressed; 2026-09-04 is left as an operator decision, outside the three-day lookback. **2026-09-18 now: 7 calendar events, 6 business lectures, 5 complete, 1 waiting on attendance, 1 suppressed duplicate, 0 review, 0 failed.** The winner's stage matrix is byte-identical either side of the write. See [phase4c1_duplicate_event_resolution.md](phase4c1_duplicate_event_resolution.md).
   - **Phase 3C3 - Wider cutover (not started, blocked on Gates A, B, D, E, J and L).**
3. **QA Engine Migration** — port QA analysis and controlled compatibility writes after validation. Not implemented.
4. **Positive Clips Migration** — port detection/orchestration without changing asset identity prematurely. Not implemented.
5. **Lecture Parts + Direct Media Pipeline** — integrate the existing planner, queue, FFmpeg worker, and SharePoint delivery. Not implemented.
6. **Scheduler / Retry / Reconciliation** — add production scheduling, operational retry, and reconciliation. **Implemented in Phase 4A and validated; `SCHEDULER_ENABLED` is still false pending the go-ahead to turn it on.**
7. **Controlled production cutover** — compare shadow evidence, approve ownership changes, and retire n8n paths incrementally. Not implemented.

## Phase 2 entry gate

Satisfied as of 2026-09-16: the calendar owner is configured, the 2026-09-04 shadow runs are reviewed, JoinWebUrl/Aptem policy is settled (canonical registry = active-Aptem-matched Teams occurrences only), and registry identity stability is proven idempotent. Unresolved occurrences are retained and gated by `downstream_ready` rather than merged or deleted. The first Phase 2 change should be an additive `lecture_transcript_artifacts` design keyed by its own artifact ID with a foreign key to `lecture_sessions.lecture_id`; acquisition must remain shadow-only and must not write legacy QA tables.

