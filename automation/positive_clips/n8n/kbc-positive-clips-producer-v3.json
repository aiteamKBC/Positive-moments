{
  "name": "KBC Positive Clips - Producer v3 (explicit origin)",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "session_id",
              "type": "string"
            },
            {
              "name": "mode",
              "type": "string"
            },
            {
              "name": "allow_origin_override",
              "type": "boolean"
            }
          ]
        }
      },
      "id": "d0000000-0000-4000-8000-000000000001",
      "name": "Called By V5 Analysis",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        -460,
        0
      ],
      "notes": "mode = 'live' or 'history'. allow_origin_override is an administrative escape hatch; leave false."
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "p1",
              "name": "session_ids",
              "value": "={{ $json.session_id ? '{\"' + $json.session_id + '\"}' : '{}' }}",
              "type": "string"
            },
            {
              "id": "p2",
              "name": "clip_keys",
              "value": "={}",
              "type": "string"
            },
            {
              "id": "p3",
              "name": "mode",
              "value": "={{ $json.mode === 'live' ? 'live' : 'history' }}",
              "type": "string"
            },
            {
              "id": "p4",
              "name": "allow_origin_override",
              "value": "={{ $json.allow_origin_override === true }}",
              "type": "boolean"
            },
            {
              "id": "p5",
              "name": "produced_by",
              "value": "positive-clip-producer-v3",
              "type": "string"
            },
            {
              "id": "p6",
              "name": "destination_drive_id",
              "value": "={{ $vars.KBC_CLIPS_DEST_DRIVE_ID }}",
              "type": "string"
            },
            {
              "id": "p7",
              "name": "destination_folder_item_id",
              "value": "={{ $vars.KBC_CLIPS_DEST_FOLDER_ITEM_ID }}",
              "type": "string"
            }
          ]
        },
        "options": {}
      },
      "id": "d0000000-0000-4000-8000-000000000002",
      "name": "Producer Parameters",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        -250,
        0
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH requested AS (\n    SELECT unnest($1::text[]) AS session_id\n),\ncurrent AS (\n    SELECT r.session_id,\n           s.session_id IS NOT NULL AS exists,\n           s.clips_media_origin     AS stored_origin,\n           s.clips_analysis_completeness AS analysis_status\n    FROM requested r\n    LEFT JOIN public.qa_doctors_sessions s ON s.session_id = r.session_id\n),\ndecided AS (\n    SELECT c.*,\n           CASE\n               WHEN NOT c.exists                               THEN 'not_found'\n               WHEN c.stored_origin IS NULL                    THEN 'set'\n               WHEN c.stored_origin = $2::text           THEN 'unchanged'\n               WHEN $3::boolean                THEN 'overridden'\n               ELSE 'conflict'\n           END AS outcome\n    FROM current c\n),\napplied AS (\n    UPDATE public.qa_doctors_sessions s\n    SET clips_media_origin = $2::text\n    FROM decided d\n    WHERE s.session_id = d.session_id\n      AND d.outcome IN ('set', 'overridden')\n    RETURNING s.session_id\n)\nSELECT\n    d.session_id,\n    $2::text        AS requested_mode,\n    d.stored_origin       AS previous_origin,\n    d.analysis_status,\n    d.outcome,\n    CASE WHEN d.outcome IN ('set', 'overridden', 'unchanged') THEN $2::text\n         ELSE d.stored_origin END AS effective_origin,\n    CASE WHEN d.outcome IN ('set', 'overridden', 'unchanged')\n         THEN (CASE WHEN $2::text = 'live' THEN 100 ELSE 10 END)\n         ELSE NULL END    AS effective_priority\nFROM decided d\n",
        "options": {
          "queryReplacement": "={{ $json.session_ids }},{{ $json.mode }},{{ $json.allow_origin_override }}"
        }
      },
      "id": "d0000000-0000-4000-8000-000000000003",
      "name": "Persist Session Media Origin",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        -40,
        0
      ],
      "credentials": {},
      "notes": "STEP 1 - must run before any job is created. Outcome per session: set / unchanged / conflict / overridden / not_found."
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.outcome }}",
              "rightValue": "conflict,not_found",
              "operator": {
                "type": "string",
                "operation": "notIn"
              }
            }
          ]
        },
        "options": {}
      },
      "id": "d0000000-0000-4000-8000-000000000004",
      "name": "Origin Accepted?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        180,
        0
      ],
      "notes": "False branch = the stored origin disagrees with the requested mode. Never silently converted."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH expanded AS (\n    SELECT\n        s.session_id,\n        s.date                         AS lecture_date,\n        s.subject,\n        s.trainer,\n        s.recording_drive_id,\n        s.recording_item_id,\n        s.clips_media_origin           AS media_origin,\n        c.ord::int                     AS clip_index,\n        c.clip\n    FROM public.qa_doctors_sessions s\n    CROSS JOIN LATERAL jsonb_array_elements(s.positive_clips) WITH ORDINALITY AS c(clip, ord)\n    WHERE s.clips_analysis_completeness = 'positive_clips_v5_final'\n      AND jsonb_typeof(s.positive_clips) = 'array'\n      AND jsonb_array_length(s.positive_clips) > 0\n      AND coalesce(s.recording_drive_id, '') <> ''\n      AND coalesce(s.recording_item_id, '') <> ''\n      AND coalesce(s.session_id, '') <> ''\n      AND (cardinality($3::text[]) = 0 OR s.session_id = ANY($3::text[]))\n),\nparsed AS (\n    SELECT\n        e.*,\n        e.clip ->> 'start'                       AS original_start,\n        e.clip ->> 'end'                         AS original_end,\n        e.clip ->> 'start_cue'                   AS start_cue_txt,\n        e.clip ->> 'end_cue'                     AS end_cue_txt,\n        (e.clip ->> 'start') ~ '^\\d{1,3}:[0-5]\\d:[0-5]\\d\\.\\d{1,3}$' AS start_ts_valid,\n        (e.clip ->> 'end')   ~ '^\\d{1,3}:[0-5]\\d:[0-5]\\d\\.\\d{1,3}$' AS end_ts_valid,\n        (e.clip ->> 'start_cue') ~ '^\\d+$'       AS start_cue_valid,\n        (e.clip ->> 'end_cue')   ~ '^\\d+$'       AS end_cue_valid\n    FROM expanded e\n),\ncomputed AS (\n    SELECT\n        p.*,\n        CASE WHEN p.start_ts_valid THEN\n            round(EXTRACT(EPOCH FROM p.original_start::interval)::numeric, 3) END AS orig_start_seconds,\n        CASE WHEN p.end_ts_valid THEN\n            round(EXTRACT(EPOCH FROM p.original_end::interval)::numeric, 3)   END AS orig_end_seconds\n    FROM parsed p\n),\npadded AS (\n    SELECT\n        c.*,\n        GREATEST(0::numeric, c.orig_start_seconds - 60) AS start_seconds,\n        c.orig_end_seconds + 60                         AS end_seconds,\n        c.start_cue_txt::int                            AS start_cue,\n        c.end_cue_txt::int                              AS end_cue\n    FROM computed c\n    WHERE c.start_ts_valid AND c.end_ts_valid\n      AND c.start_cue_valid AND c.end_cue_valid\n      AND c.orig_end_seconds > c.orig_start_seconds     -- original_end > original_start\n),\nkeyed AS (\n    SELECT\n        p.*,\n        p.session_id || ':' || p.start_cue || ':' || p.end_cue                    AS clip_key,\n        'positive:' || left(md5(p.session_id), 8) || ':' || p.start_cue || ':' || p.end_cue AS job_key,\n        to_char(p.lecture_date::date, 'YYYYMMDD') || '_'\n          || left(trim(BOTH '-' FROM regexp_replace(coalesce(p.subject, 'lecture'), '[^A-Za-z0-9]+', '-', 'g')), 60) || '_'\n          || left(md5(p.session_id), 8) || '_clip-'\n          || lpad(p.clip_index::text, 2, '0') || '_cue-'\n          || p.start_cue || '-' || p.end_cue || '.mp4'                            AS output_filename\n    FROM padded p\n    WHERE p.start_seconds >= 0\n      AND p.end_seconds > p.start_seconds\n),\ncandidates AS (\n    SELECT\n        k.*,\n        round(k.end_seconds - k.start_seconds, 3) AS requested_duration_seconds,\n        a.trim_status AS existing_asset_status,\n        j.status      AS existing_job_status\n    FROM keyed k\n    LEFT JOIN public.qa_positive_clip_assets a ON a.clip_key = k.clip_key\n    LEFT JOIN LATERAL (\n        SELECT mj.status FROM public.qa_media_jobs mj\n        WHERE mj.clip_key = k.clip_key\n        ORDER BY CASE mj.status WHEN 'completed' THEN 1 WHEN 'processing' THEN 2\n                                WHEN 'pending' THEN 3 ELSE 4 END\n        LIMIT 1\n    ) j ON TRUE\n    WHERE (cardinality($4::text[]) = 0 OR k.clip_key = ANY($4::text[]))\n)\n\nINSERT INTO public.qa_media_jobs (\n    job_key, session_id, job_type, part_number, clip_key,\n    start_seconds, end_seconds, cut_mode, output_filename,\n    source_drive_id, source_item_id,\n    destination_drive_id, destination_folder_item_id,\n    status, attempt_count, priority, not_before, metadata\n)\nSELECT\n    c.job_key,\n    c.session_id,\n    'positive_clip',\n    NULL,\n    c.clip_key,\n    c.start_seconds,\n    c.end_seconds,\n    'precise',\n    c.output_filename,\n    c.recording_drive_id,\n    c.recording_item_id,\n    $1,\n    $2,\n    'pending',\n    0,\n    CASE c.media_origin WHEN 'live' THEN 100::smallint ELSE 10::smallint END,\n    NOW(),\n    jsonb_strip_nulls(jsonb_build_object(\n        'lecture_date',           to_char(c.lecture_date::date, 'YYYY-MM-DD'),\n        'subject',                c.subject,\n        'trainer',                c.trainer,\n        'clip_index',             c.clip_index,\n        'original_start',         c.original_start,\n        'original_end',           c.original_end,\n        'original_duration_seconds', round(c.orig_end_seconds - c.orig_start_seconds, 3),\n        'start_cue',              c.start_cue,\n        'end_cue',                c.end_cue,\n        'speaker',                c.clip ->> 'speaker',\n        'positive_speakers',      c.clip -> 'positive_speakers',\n        'category',               c.clip ->> 'category',\n        'positive_quote',         c.clip ->> 'positive_quote',\n        'quote',                  c.clip ->> 'quote',\n        'reason',                 c.clip ->> 'reason',\n        'confidence',             c.clip -> 'confidence',\n        'semantic_verification',  c.clip -> 'semantic_verification',\n        'padding_before_seconds', 60,\n        'padding_after_seconds',  60,\n        'queue_origin',           c.media_origin,\n        'produced_by',            $5::text,\n        'produced_at',            to_char(NOW() AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS\"Z\"')\n    ))\nFROM candidates c\nWHERE\n    (c.existing_asset_status IS DISTINCT FROM 'completed')\n    AND (c.existing_job_status IS NULL OR c.existing_job_status NOT IN ('pending', 'processing', 'completed'))\n    AND c.media_origin IN ('history', 'live')\nON CONFLICT (job_key) DO NOTHING\nRETURNING job_id, job_key, clip_key, session_id, job_type, status, priority,\n          start_seconds, end_seconds,\n          round(end_seconds - start_seconds, 3) AS duration_seconds,\n          cut_mode, output_filename, metadata ->> 'queue_origin' AS queue_origin\n",
        "options": {
          "queryReplacement": "={{ $('Producer Parameters').first().json.destination_drive_id }},{{ $('Producer Parameters').first().json.destination_folder_item_id }},{{ $('Producer Parameters').first().json.session_ids }},{{ $('Producer Parameters').first().json.clip_keys }},{{ $('Producer Parameters').first().json.produced_by }}"
        }
      },
      "id": "d0000000-0000-4000-8000-000000000005",
      "name": "Queue Positive Clip Jobs",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        400,
        -110
      ],
      "credentials": {},
      "notes": "STEP 2 - priority is derived from the lecture's persisted clips_media_origin, never from the lecture date.",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "errorMessage": "=Origin conflict for session {{ $json.session_id }}: stored '{{ $json.previous_origin }}' but mode '{{ $json.requested_mode }}' was requested. Nothing was changed or queued."
      },
      "id": "d0000000-0000-4000-8000-000000000006",
      "name": "Stop On Origin Conflict",
      "type": "n8n-nodes-base.stopAndError",
      "typeVersion": 1,
      "position": [
        400,
        120
      ]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "s1",
              "name": "queued_count",
              "value": "={{ $input.all().filter(i => i.json.job_id).length }}",
              "type": "number"
            },
            {
              "id": "s2",
              "name": "priorities",
              "value": "={{ [...new Set($input.all().filter(i => i.json.job_id).map(i => i.json.priority))] }}",
              "type": "array"
            },
            {
              "id": "s3",
              "name": "job_keys",
              "value": "={{ $input.all().filter(i => i.json.job_id).map(i => i.json.job_key) }}",
              "type": "array"
            }
          ]
        },
        "options": {}
      },
      "id": "d0000000-0000-4000-8000-000000000007",
      "name": "Summarise Queued Jobs",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        640,
        -110
      ]
    }
  ],
  "connections": {
    "Called By V5 Analysis": {
      "main": [
        [
          {
            "node": "Producer Parameters",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Producer Parameters": {
      "main": [
        [
          {
            "node": "Persist Session Media Origin",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Persist Session Media Origin": {
      "main": [
        [
          {
            "node": "Origin Accepted?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Origin Accepted?": {
      "main": [
        [
          {
            "node": "Queue Positive Clip Jobs",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Stop On Origin Conflict",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Queue Positive Clip Jobs": {
      "main": [
        [
          {
            "node": "Summarise Queued Jobs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false,
  "pinData": {},
  "tags": [
    {
      "name": "kbc-media"
    }
  ]
}